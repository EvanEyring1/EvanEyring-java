import java.util.ArrayList;
    public class worksheet {
        public static void main(String[] args) {
            //Ex. 1a.
            14, 6, 2200, 96

            //Ex. 1b.
            4, 99, 2, 44, 11

            //Ex. 1c.
            3, -18, 6, 3, 9

            //Ex. 1d
            int[] arrayOne = {10, -2, 4, -4, 9, -5, 19, -7, 39, -1};
            int max = arrayOne[0];
            for (int x : arrayOne) {
                max = Math.max(arrayOne[x], max);
            }
            //Ex. 1e.
            int average;
            for (int x : arrayOne) {
                average = Math.max(x1, x2);

                //Ex. 2a.
                n = 0;
                if (n == a.length)
                    arrayNumbers[] h;
                h = new double[2 * a.length];
                for (int i = 0; i < a.length; i++)
                    h[i] = a[i];
                a = h
            }
            a[n] = max
            n++


            }
            //Ex. 2b


        }
    }